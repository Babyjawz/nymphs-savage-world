# Default: track archives with LFS
*.7z  filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Submissions are light by default (no LFS) so web UI uploads work (<= 25 MB)
Submissions/** -filter -diff -merge text

# Heavy corner under Submissions that DOES use LFS
Submissions/LFS/** filter=lfs diff=lfs merge=lfs -text
